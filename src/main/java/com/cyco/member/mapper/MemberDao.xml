<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cyco.member.dao.MemberDao">
	
	<!-- 멤버리스트 최초로딩-->
	<select id="getMemberList" resultType="com.cyco.member.vo.MemberVo">
		select rownum,
			   m_id,
			   m_nick,
			   m_image,
			   position,
			   skill,
			   p_enabled,
			   s_enabled
		from V_MLIST
	</select>

	<select id="getPositionList" resultType="com.cyco.member.vo.PositionVo">
		select position_id, position_name, position_enabled
		from position
		order by position_id
	</select>
	
	<!-- getMemberPosition -->
	<select id="getMemberPosition" resultType="com.cyco.member.vo.MemberVo">
		select rownum, m_id, m_nick, m_image, position, skill, p_enabled, s_enabled
		from V_MLIST
		where position like '%${position}%'
	</select>
	
	<!-- getMemberNickname -->
	<select id="getMemberNickname" resultType="com.cyco.member.vo.MemberVo">
		select rownum, m_id, m_nick, m_image, position, skill, p_enabled, s_enabled
		from V_MLIST
		where m_nick like '%${nickname}%'
	</select>

</mapper>